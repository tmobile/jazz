const assert = require('chai').assert;
const index = require('..');

describe('Sample', () => {
  it('tests handler', (done) => {

    // Add your test cases here.
    assert(true);
    done();
  });
});
