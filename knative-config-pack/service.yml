apiVersion: serving.knative.dev/v1alpha1
kind: Service
metadata:
  name: <service>
  namespace: default
spec:
  runLatest:
    configuration:
      revisionTemplate:
        spec:
          container:
            image: docker.io/dbabu/<service>
            env:
            - name: TARGET
              value: "Jazz hello world"